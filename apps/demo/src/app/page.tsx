import { FreeformTemplate } from '@/templates/freeform'

/**
 * Default demo: full-screen freeform drawing canvas.
 * The template is self-contained — swap FreeformTemplate for any other
 * template in src/templates/ to demo different configurations.
 */
export default function Page() {
  return <FreeformTemplate />
}
